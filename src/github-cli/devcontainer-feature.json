{
  "id": "github-cli",
  "version": "1.0.0",
  "name": "GitHub CLI",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version of GitHub CLI to install (gh <version>)"
    }
  }
}
