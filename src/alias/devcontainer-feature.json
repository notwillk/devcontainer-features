{
  "id": "alias",
  "version": "1.0.0",
  "name": "Alias",
  "description": "Create a simple wrapper that forwards to another command.",
  "options": {
    "name": {
      "type": "string",
      "default": "my-alias",
      "description": "Name of the wrapper command to create"
    },
    "exec": {
      "type": "string",
      "default": "echo 'hello from alias'",
      "description": "Command that will be executed"
    }
  }
}
