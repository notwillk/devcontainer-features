{
  "id": "ssh-server",
  "version": "1.0.0",
  "name": "SSH Server",
  "description": "Install and start an OpenSSH server for the configured devcontainer user.",
  "init": true,
  "postStartCommand": "sudo -E /usr/local/share/devcontainer-ssh-server/start.sh",
  "options": {
    "port": {
      "type": "string",
      "default": "22",
      "description": "Port sshd listens on inside the container"
    }
  }
}
