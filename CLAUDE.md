# Claude Instructions

## Adding a new feature

When adding a new feature to `src/`, always update `README.md` to include it in the features table. The table is sorted alphabetically by tool name and has columns: tool name, feature reference (`ghcr.io/notwillk/devcontainer-features/<id>`), and options (comma-separated option names, or `-` if none).
