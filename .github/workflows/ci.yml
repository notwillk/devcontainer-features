name: Test on PR

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test-features:
    uses: ./.github/workflows/test-features.yml
    with:
      test_script: "bash \"src/<FEATURE>/test.sh\""